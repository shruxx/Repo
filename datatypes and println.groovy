class Example
{ 
  static void main(String[] args)
  {
    //Beispiel für einen int Datentypen
    int x=5;
    //Beispiel für einen long Datentypen
    long y=100L;
    //Beispiel für einen float Datentypen
    float a=10.56f;
    //Beispiel für einen double Datentypen
    double b=10.5e40;
    //Beispiel für einen BigInteger Datentypen
    BigInteger bi=30g;
    //Beispiel für einen BigDecimal Datentypen
    BigDecimal bd=3.5g;
    println('Das sind die Ausgabebeispiele für einzelne Datentypen');
    println 'Beispiel für einen int Datentypen';
    println(x);
    println 'Beispiel für einen long Datentypen';
    println(y);
    println 'Beispiel für einen float Datentypen';
    println(a);
    println 'Beispiel für einen double Datentypen';
    println(b);
    println 'Beispiel für einen BigInteger Datentypen';
    println(bi);
    println 'Beispiel für einen BigDecimal Datentypen';
    println(bd);
    
     
    
  }
}